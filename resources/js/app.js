import './bootstrap';

// Optional GSAP / Lenis integration can be added here later.
// Example:
// import gsap from 'gsap';
// import { ScrollTrigger } from 'gsap/ScrollTrigger';
// gsap.registerPlugin(ScrollTrigger);
// Then animate hero, cards, section reveals, and sticky transitions.